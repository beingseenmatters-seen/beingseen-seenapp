# Moment Library CDN (GLOBAL)

Stable publish root for production clients.

`VITE_MOMENT_LIBRARY_GLOBAL_BASE` must point at the `global/` directory URL
(same path for v1 and v2 — only contents change).

CN is not published on this branch.
