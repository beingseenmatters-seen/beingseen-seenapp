# Moment Library CDN (GLOBAL) — Official

Stable publish root for production clients.

`VITE_MOMENT_LIBRARY_GLOBAL_BASE` points at `global/`.

Current: **libraryVersion 2** (FRI-002, PAR-001).

`library.v1.pack.json` retained for rollback (flip `manifest.json` only if needed).
CN is not published here.
